# VULNERABILITY: Outdated dependencies with known CVEs (for Snyk/Trivy)
Flask==1.1.2
PyYAML==5.1
gunicorn==20.1.0

# PINNED DEPENDENCY: Pin Jinja2 to a version compatible with Flask 1.1.2
# to resolve the "cannot import name 'escape'" ImportError.
Jinja2==3.0.3